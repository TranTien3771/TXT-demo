#include <bits/stdc++.h>

using namespace std;

int s[1000][1000];

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n; cin >> n;
    for (int i = 0; i < n; i++){
    	for (int j = 0; j < n; j++){
    		cin >> s[i][j];
		}
	}
	int sum = 0;
	int mid = n/2;
	for (int i = mid; i < n; i++){
		for (int j = mid; j < n; j++){
			
		}
	}
    return 0;
}
